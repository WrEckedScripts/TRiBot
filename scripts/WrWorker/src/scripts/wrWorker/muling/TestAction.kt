package scripts.wrWorker.muling

class TestAction {
    val action = "trade"
    val subject = "name"
    val items = mapOf(
        "Iron ore" to 100,
        "Coal" to 100
    )
    val coordinates = mapOf(
        "x" to 123,
        "y" to 321,
        "plane" to 0,
    )
}